{
  "frontend/src/index.css": "Global styles with professional blue OKLCH color palette, Inter/Playfair Display fonts, forced dark text on form inputs, and updated print media query that hides all UI except the resume preview container.",
  "frontend/tailwind.config.js": "Tailwind config extended with Inter/Playfair Display fonts, blue color tokens mapped to CSS variables, card shadow utilities, and accordion/fade/slide-up animations.",
  "frontend/src/types/resume.ts": "TypeScript types for resume form data with a self-contained `personalInfo` interface (no backend type extension) ensuring all fields are plain strings with no undefined values, plus factory defaults for all resume sections.",
  "frontend/src/utils/sessionToken.ts": "Utility to generate and persist a random hex session token in localStorage for anonymous resume persistence.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for fetching (useGetResume) and saving (useSaveResume) resume data to the backend using the session token, with cache invalidation on save.",
  "frontend/src/hooks/useResumeData.ts": "Custom hook managing resume form state with localStorage persistence, backend sync on mount, and debounced auto-save — now with robust `loadFromLocalStorage` that merges stored data with defaults to ensure all fields (especially optional `linkedin`/`github`/`photoUrl`) are always strings, never undefined.",
  "frontend/src/components/PersonalInfoSection.tsx": "Form section for personal info fields with all controlled inputs using `?? ''` fallback to guarantee string values even if the data object has undefined optional fields.",
  "frontend/src/components/EducationSection.tsx": "Repeatable form section for education entries (degree, major, institution, graduation year) with add/remove controls.",
  "frontend/src/components/ExperienceSection.tsx": "Repeatable form section for work experience entries (job title, company, start/end dates, description) with add/remove controls.",
  "frontend/src/components/SkillsSection.tsx": "Form section for skills with name input and proficiency level select dropdown, supporting multiple entries with add/remove.",
  "frontend/src/components/ProjectsSection.tsx": "Repeatable form section for projects (name, tech stack, link, description) with add/remove controls.",
  "frontend/src/components/CertificationsSection.tsx": "Repeatable form section for certifications (name, issuer, issue date, expiry date) with add/remove controls.",
  "frontend/src/components/ProfilePhotoSection.tsx": "Profile photo upload component using native browser `URL.createObjectURL` for local preview (no external blob storage dependency), with circular preview display, remove button, and proper object URL cleanup on unmount.",
  "frontend/src/components/ResumeForm.tsx": "Main form container using shadcn Accordion to organize all resume sections (photo, personal info, education, experience, skills, projects, certifications) with amber accent icons.",
  "frontend/src/components/ResumePreview.tsx": "Named-export inline-styled resume preview component (id=\"resume-preview-container\") rendering all sections (header with photo, contact, summary, experience, education, skills, projects, certifications) using the correct local `Project` type fields (`name`, `techStack`, `link`) instead of backend `Project` fields.",
  "frontend/src/App.tsx": "Main app component with sticky header, split form+preview layout, save and \"Save as HTML\" actions, mobile FAB, and branded footer — updated to pass `resumeData` to `useDownloadPDF`, call `downloadHTML`, and label the button \"Save as HTML\".",
  "frontend/src/components/WelcomeScreen.tsx": "Full-page blue-themed welcome screen with \"Welcome to Resume Builder\" heading, feature highlights grid, and a prominent \"Get Started\" CTA button — with the invalid `focusRingColor` inline style property removed.",
  "frontend/src/hooks/useDownloadPDF.ts": "Custom hook that exports resume data as a fully self-contained `.html` file — all styles are inlined, profile photo is converted to a base64 data URL, and the download is triggered via a programmatic anchor click. Exposes `downloadHTML` (and `downloadPDF` alias), plus `isGenerating` state; accepts `formData` as a parameter."
}