{
  "frontend/src/index.css": "Global styles with blue OKLCH theme; updated `--border`/`--input` tokens to be more visible (darker), added always-visible border, background, and text-color rules for all inputs/textareas/selects at rest (not just on hover), with proper dark-mode variants and SelectTrigger visibility fix. Preserves the `.text-glow-blue` utility class.",
  "frontend/tailwind.config.js": "Tailwind config extended with Inter/Playfair Display fonts, blue color tokens mapped to CSS variables, card shadow utilities, and accordion/fade/slide-up animations.",
  "frontend/src/types/resume.ts": "TypeScript types for resume form data with a self-contained `personalInfo` interface (no backend type extension) ensuring all fields are plain strings with no undefined values, plus factory defaults for all resume sections.",
  "frontend/src/utils/sessionToken.ts": "Utility to generate and persist a random hex session token in localStorage for anonymous resume persistence.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for fetching (useGetResume) and saving (useSaveResume) resume data to the backend using the session token, with cache invalidation on save.",
  "frontend/src/hooks/useResumeData.ts": "Custom hook managing resume form state with localStorage persistence, backend sync on mount, and debounced auto-save — now with robust `loadFromLocalStorage` that merges stored data with defaults to ensure all fields (especially optional `linkedin`/`github`/`photoUrl`) are always strings, never undefined.",
  "frontend/src/components/PersonalInfoSection.tsx": "Form section for personal info fields; all Input and Textarea components now explicitly include `border border-border bg-white text-gray-900` classes to ensure they are always visually distinct and legible regardless of hover state.",
  "frontend/src/components/EducationSection.tsx": "Repeatable form section for education entries; all Input components now include `border border-border bg-white text-gray-900` classes for always-visible styling.",
  "frontend/src/components/ExperienceSection.tsx": "Repeatable form section for work experience entries; all Input and Textarea components now include `border border-border bg-white text-gray-900` classes for always-visible styling.",
  "frontend/src/components/SkillsSection.tsx": "Form section for skills with name input and proficiency select; Input now has `border border-border bg-white text-gray-900` and SelectTrigger has `border border-border bg-white text-gray-900` for always-visible styling.",
  "frontend/src/components/ProjectsSection.tsx": "Repeatable form section for projects; all Input and Textarea components now include `border border-border bg-white text-gray-900` classes for always-visible styling.",
  "frontend/src/components/CertificationsSection.tsx": "Repeatable form section for certifications; all Input components now include `border border-border bg-white text-gray-900` classes for always-visible styling.",
  "frontend/src/components/ProfilePhotoSection.tsx": "Profile photo upload component; unchanged in logic, minor text color fix using `text-card-foreground` for the \"Profile Photo\" label to ensure visibility.",
  "frontend/src/components/ResumeForm.tsx": "Main form container using shadcn Accordion for all resume sections, with all section title spans now using the `.text-glow-blue` CSS utility class to render a visible blue glow effect on titles like \"Personal Info\", \"Education\", etc.",
  "frontend/src/components/ResumePreview.tsx": "Inline-styled resume preview component accepting `formData` and optional `previewRef` props, forwarding the ref to the outermost container div to enable image capture, rendering all resume sections with explicit dark text colors for full visibility.",
  "frontend/src/App.tsx": "Main app component with sticky white header (explicit dark text colors via inline styles), blue-tinted preview panel wrapper, blue-styled footer with light text, and all action buttons — correctly wired to useResumeData and download hooks.",
  "frontend/src/components/WelcomeScreen.tsx": "Full-page blue-themed welcome screen with \"Welcome to Resume Builder\" heading, feature highlights grid, and a prominent \"Get Started\" CTA button — with the invalid `focusRingColor` inline style property removed.",
  "frontend/src/hooks/useDownloadPDF.ts": "Custom hook that exports resume data as a fully self-contained `.html` file — all styles are inlined, profile photo is converted to a base64 data URL, and the download is triggered via a programmatic anchor click. Exposes `downloadHTML` (and `downloadPDF` alias), plus `isGenerating` state; accepts `formData` as a parameter.",
  "frontend/src/hooks/useDownloadImage.ts": "Custom hook that captures the resume preview DOM element as a PNG image using SVG foreignObject technique and triggers a browser download as 'resume.png', with loading state management and cross-origin image handling."
}